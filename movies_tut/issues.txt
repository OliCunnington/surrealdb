RETURNS inside {}
	date_to_time
	month_to_num
	ratings funcs

adding schema
	COMPUTE not a thing, <future> available, but unable to get syntax correct

Using the wrong fucking version... 3.0.0 beta only avail linux?

Ubuntu VM running with SurrealDB installed and running
able to ssh in...

dafuq i do now?
lol

ofcun@LAPTOP-KSI9D0JN:~$ surreal sql --namespace ns --database db --username root --password root
2026-01-21T13:38:37.323164Z ERROR surreal::cli: There was a problem with the database: There was an error processing a remote WS request: WebSocket protocol error: SubProtocol error: Server sent no subprotocol

22/1
	today... it just worked?
	ns/db created from cli...

	{WebSocket learnings is needed}

	now we files?
	yay... scp -r
	but now we .../movie_tut/movies_tut

	movies_naive.surql done,
	cannot seem to connect to app.surrealdb, probably VM port forward needed


its linux time...

mem stick to back up some pics and code ?

code on git... so just pics?

--port froward and port host tests, all failed
-- WS error for 8123... default is fine (8000), but no app.surrealdb.com connect
-- localhost, 127.0.0.1, 127.0.1.1, 0.0.0.0 tried
-- 
-- deleted WSL Bridge portforward rule in win-firewall

mint tomorrow?

-- stick needed... i think
-- poked around in ozzaks migration tool repo